In this version, the embedding is computed so that two traces close in the embedding suffer from the same interferences with respect to a set of references traces.
Then, comparing the sensibility of two traces boils down to computing the cosine similary between the two embeddings.
